# --- Core runtime (required; the system runs OFFLINE with just these) --------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
rank-bm25>=0.2.2          # lexical retriever (pure-python fallback exists)
numpy>=1.26               # dense vector store (pure-python fallback exists)
langgraph>=0.2            # state machine (sequential fallback exists)

# --- Testing / tooling -------------------------------------------------------
pytest>=8.0
httpx>=0.27               # test client / async HTTP

# --- Optional: real models & backends (NOT needed for the offline demo) ------
# Everything above degrades gracefully to deterministic mocks. Install these
# only to enable real embeddings/reranking/LLM or a Qdrant backend, and flip
# the matching env flags (USE_REAL_EMBEDDINGS=1, USE_REAL_RERANKER=1,
# USE_REAL_LLM=1, VECTOR_BACKEND=qdrant).
#
# sentence-transformers>=2.7   # all-MiniLM-L6-v2 embeddings + cross-encoder
# torch>=2.2                   # backend for sentence-transformers
# qdrant-client>=1.9           # optional vector backend
# anthropic>=0.34              # default real LLM provider (claude-3-5-sonnet)
# openai>=1.30                 # optional real LLM provider
